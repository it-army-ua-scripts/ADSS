#!/sbin/openrc-run

name="db1000n"
pidfile="/run/${name}.pid"
tool_bin="/opt/itarmy/bin/${name}"
output_log="/var/log/adss.log"
args=""

depend()
{
        after net
}

start() {
        ebegin "Start ${name} ddos tool"
        ${tool_bin} ${args} > ${output_log} 2>&1
        eend $?
}
